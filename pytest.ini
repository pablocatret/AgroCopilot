[pytest]
testpaths =
    tests
    tests_evaluation
norecursedirs =
    .venv
    node_modules
    test-tmp
    .cache
markers =
    integration: tests that require external services or network access
